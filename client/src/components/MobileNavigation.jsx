import {NavLink} from 'react-router-dom';import {Home,Image,Plus,Users,Menu} from 'lucide-react';
export default function MobileNavigation(){return <nav className="lg:hidden fixed bottom-0 inset-x-0 z-40 bg-white border-t flex justify-around py-2 pb-[max(.5rem,env(safe-area-inset-bottom))]">{[['/',Home,'Home'],['/memories',Image,'Memories'],['/add/memory',Plus,'Add'],['/family-tree',Users,'Family'],['/settings',Menu,'More']].map(([to,I,l])=><NavLink to={to} key={to} className="flex flex-col items-center min-w-14 text-[11px] text-stone-600"><I className="w-5 h-5 mb-1"/>{l}</NavLink>)}</nav>}